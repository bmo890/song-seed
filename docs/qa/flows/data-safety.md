# QA Flows — Backup, Restore, Import/Export & Settings

| ID | Flow | Steps (terse) | Expected result | Auto | Platforms | Pri |
|---|---|---|---|---|---|---|
| DS-01 | Backup happy path (Android SAF) | Settings → Library & Backups → Back up → confirm dialog → Back Up → progress takeover → pick folder | Confirm shows "N recordings · size · time" estimate; zip lands in folder; "Backup ready" alert w/ filename + Copy Name; card meta "Last backup · time" + Verified chip; filename row appears | AUTO-A | Android | P1 |
| DS-02 | Backup happy path (iOS share sheet) | Back up → Back Up → share sheet → Save to Files → "Confirm backup saved" dialog → I Saved It | Zip in Files/iCloud; success alert w/ filename; Verified chip + last-backup meta only recorded AFTER "I Saved It"; "Not Saved" records nothing | HUMAN | iOS | P1 |
| DS-03 | Backup confirm declined | Back up → confirm dialog → Not Now | Dialog closes; no process starts; last-backup meta unchanged | AUTO | iOS+Android | P2 |
| DS-04 | Android folder-picker backout | Back up → Back Up → back out of SAF picker | "Save this backup?" dialog (Discard Backup / Choose Location); Choose Location reopens picker WITHOUT rebuilding archive; Discard records no success | AUTO-A | Android | P1 |
| DS-05 | Cancel backup mid-run | Seed 50+ clips → Back up → Back Up → Cancel in progress takeover during build | Process dismissed silently; no error alert; no last-backup meta update | AUTO | iOS+Android | P2 |
| DS-06 | Minimize backup, keep working | Start backup → minimize takeover → browse app → reopen via bottom pill | Pill shows live progress; app fully usable; reopened takeover shows same operation; backup completes normally | AUTO | iOS+Android | P2 |
| DS-07 | Incomplete backup names missing recordings | Delete one clip's audio file from app storage (adb run-as) → Back up → save | "Backup saved, but incomplete" alert names the missing song/clip (≤6 lines, "…and N more"), points to Storage details; no Verified/meta update | AUTO-A | iOS+Android | P1 |
| DS-08 | Backup with low device storage | Fill device storage → Back up | Friendly "Your device is low on storage" message (not raw ENOSPC); process shows error state; no partial success recorded | HUMAN | iOS+Android | P2 |
| DS-09 | Single-slot operation guard | Start backup → tap Back up again; tap Restore; try Export → Generate | Second Back up no-ops; Restore card disabled; export shows "One at a time" alert; nothing clobbers the running process | AUTO | iOS+Android | P3 |
| DS-10 | Copy last backup filename | After successful backup → tap copy icon on filename row | Toast "Backup file name copied"; clipboard holds exact filename | AUTO | iOS+Android | P3 |
| DS-11 | Backup reminder at launch | Library w/ ≥1 idea, never backed up, reminder Monthly (default) → cold launch → Later → relaunch | "Back up your library?" prompt once per session; Later dismisses; reappears on next cold launch; NO prompt when reminder Off or library has 0 ideas | AUTO | iOS+Android | P2 |
| DS-12 | Backup reminder Pro gate (dev override) | Dev build → force free via dev Pro override → Library & Backups → tap Weekly/Monthly/Every 3 months; tap Off; force Pro → tap Weekly | Non-off options open Pro upsell sheet and selection stays unchanged; Off always selectable; with Pro, frequency changes and persists | AUTO | iOS+Android | P2 |
| DS-13 | Restore mode choice dialog | Restore → pick backup zip | Dialog "Restore from backup?" w/ size+time line, Cancel / Keep Newer Items (merge, described) / Replace Everything (destructive, "trash for 14 days" copy); Cancel leaves library untouched; backing out of file picker returns silently | AUTO-A | iOS+Android | P1 |
| DS-14 | Replace restore end-to-end | Restore → pick backup → Replace Everything → wait → Restart SongNook | Progress takeover → "Restored. Restarting…" → "Restore complete" gate → restart → library exactly matches backup; displaced recordings recoverable from trash for 14 days | AUTO-A | iOS+Android | P1 |
| DS-15 | Merge restore keeps newer work | Back up → add/edit songs → Restore same backup → Keep Newer Items → restart | Backup items return AND post-backup songs/edits survive; no duplicates of unchanged items | AUTO-A | iOS+Android | P1 |
| DS-16 | Wrong-file cross-detection | Restore → pick a SongNook Archive (export); Import an archive → pick a full backup zip | Restore: "That's a shareable archive" alert pointing to Import; Import: "That's a full backup" alert pointing to Restore; neither proceeds or calls the file corrupt | AUTO-A | iOS+Android | P2 |
| DS-17 | Corrupt zip restore | Restore → pick a garbage/renamed .zip → choose a mode | "Restore failed" + clear message; library completely untouched; no restart gate | AUTO-A | iOS+Android | P1 |
| DS-18 | Salvage restore of incomplete backup | Restore a backup whose manifest recorded missing recordings → pick mode | "Backup is incomplete" alert w/ missing count + Cancel / Restore Anyway; Restore Anyway restores everything else; success reads "Restored without N missing items. Restarting…" | AUTO-A | iOS+Android | P1 |
| DS-19 | Cancel restore mid-run | Start restore of large backup → Cancel in takeover; watch committing phase | Cancel before commit: process dismissed, library untouched; during "committing" the Cancel control is disabled (cannot abort) | AUTO-A | iOS+Android | P1 |
| DS-20 | Restore blocked while recording | Begin/pause a recording → open Settings → Library & Backups → Restore | "Finish recording first" alert; no file picker opens; recording unharmed | PARTIAL | iOS+Android | P2 |
| DS-21 | Export wizard structure + guards | Library & Backups → Export an archive → open steps 1–4; Generate w/o format; set format, Generate w/o scope | Four accordions (Format/Scope/Options/Generate) w/ live summaries in titles; "Choose a format" then "Choose a scope" alerts; Cancel button returns to Library view | AUTO | iOS+Android | P2 |
| DS-22 | Export scope selection semantics | Select workspace → expand → tap a collection (becomes exclusion) → deselect workspace; select bare collection in unselected workspace | Workspace select includes all + auto-expands; collection tap under selected workspace shows "inherited/excluded" state; sub-collections follow parent; counts in step-2 title update | AUTO | iOS+Android | P2 |
| DS-23 | Archived workspace export guard | Archive a workspace → Export → tap it (and one of its collections) in Scope | "Unarchive to export" alert both times; nothing gets selected (would silently export songs with no audio) | AUTO | iOS+Android | P2 |
| DS-24 | Export options + size estimates | Format=SongNook Archive → pick scope → toggle 5 archive options; switch to Standard ZIP | Archive: "With all metadata ≈ X · without ≈ Y" line after scope; Generate step shows "≈ size · time"; Standard ZIP shows its own 3 .txt/hidden toggles only | AUTO | iOS+Android | P3 |
| DS-25 | Export happy path (Android SAF) | Format + scope → Generate Export → takeover → pick folder | "Export saved" alert w/ counts (workspaces/collections/songs/standalone clips/notepad notes); zip in folder contains exactly the selection | AUTO-A | Android | P1 |
| DS-26 | Export happy path (iOS share sheet) | Generate Export → share sheet → Save to Files | "Export ready … handed to the share sheet" alert (no false "saved" claim); saved zip opens and contains selection | HUMAN | iOS | P1 |
| DS-27 | Export cancel paths | Cancel takeover mid-run; separately back out of Android folder picker | Both are silent no-ops: process dismissed, no failure alert, no partial file left in chosen folder | AUTO-A | iOS+Android | P2 |
| DS-28 | Export finished with warnings | Delete an in-scope clip's audio from storage → export | "Export finished with warnings" alert summarizing missing files; archive still produced | AUTO-A | iOS+Android | P2 |
| DS-29 | Import archive preview | Library & Backups → Import an archive → Choose archive → pick exported zip | Preview card: archive name, "Exported <date>", workspace/collection/song/standalone-clip/note counts, main workspace; Import/Clear rows appear | AUTO-A | iOS+Android | P1 |
| DS-30 | Import archive into library | From preview → Import into library | Spinner on row; success alert w/ imported counts; contents arrive as NEW workspaces; existing library untouched; same-name workspace gets counted suffix; imported clips show durations (backfill), not stuck at 0:00 | AUTO-A | iOS+Android | P1 |
| DS-31 | Import archive error + warnings | Pick invalid/corrupt zip; separately import archive w/ recorded warnings | Invalid: "Import failed" + message, no preview; warnings: "Import finished with warnings" listing ≤6 lines, import still lands | AUTO-A | iOS+Android | P2 |
| DS-32 | Import view cancel + mid-import lock | Load preview → Clear selection; tap Back to Settings; start import → try header back / buttons | Clear drops preview w/o importing; Back returns to Library view; during import Choose/Import/Clear/Back disabled AND header back is a no-op until finished | AUTO-A | iOS+Android | P2 |
| DS-33 | Share single audio file into app | Share an audio file from another app (adb SEND intent on Android) → Import from Share → tap current collection → answer date prompt | Screen lists the file; import runs w/ progress pill; app navigates INTO destination collection; clip present w/ correct title; back follows origin rule | AUTO-A | iOS+Android | P1 |
| DS-34 | Share multi-file → individual clips | Share 3+ audio files → pick a collection → "Individual clips" → date prompt | Choice dialog (Individual clips / Song project / Cancel); each file becomes its own clip; duplicate names get counted suffixes; pill shows n/N then done | AUTO-A | iOS+Android | P1 |
| DS-35 | Share multi-file → song project | Share 3+ files → collection → "Song project" → date prompt → title modal → Import | Modal placeholder = first file's title; one new song holding every file as takes; modal Cancel aborts with nothing imported | AUTO-A | iOS+Android | P1 |
| DS-36 | Import date preference prompt | Share/import file(s) carrying original file dates → observe prompt at each entry point | "Original date / Today's date / Cancel" w/ earliest-date preview; Original → chronology uses source date; Cancel aborts the whole import; files w/o source date skip the prompt | AUTO-A | iOS+Android | P2 |
| DS-37 | New collection from import | Share files → "New collection from import" → date prompt → name modal (leave blank) → Create | Blank name falls back to placeholder title; collection created w/ files as individual clips; if every file fails to import the empty collection is deleted and pill shows error | AUTO-A | iOS+Android | P2 |
| DS-38 | Share destination list | Open Import from Share w/ and w/o a current collection → toggle "Another collection" | Current collection row w/ path label when one exists; helper text otherwise (and Another-collection list starts expanded); other destinations show workspace eyebrow + path; row disabled states while assets resolving | AUTO-A | iOS+Android | P3 |
| DS-39 | Unsupported / mixed share payload | Share only a PDF/image; then share audio + a PDF together | Non-audio only: "does not contain supported audio", no destination section; mixed: "N shared items will be skipped" warning, only audio imports | AUTO-A | iOS+Android | P2 |
| DS-40 | Share import close/dismiss | Open Import from Share → header back w/o importing → reopen app | Share intent reset; returns to previous screen (Home on cold start); stale share does NOT reappear on next launch | AUTO-A | iOS+Android | P2 |
| DS-41 | Cold-start share | Kill app → share audio from another app | App boots directly into Import from Share w/ the file listed; completing or cancelling leaves normal navigation | AUTO-A | iOS+Android | P2 |
| DS-42 | Duplicate review — single file | Import same audio file twice (share or collection Import audio) | "Already imported" sheet; existing copy's location shown (workspace dot · collection); Skip adds nothing; "Import as copy" adds duplicate clip w/ counted title | AUTO-A | iOS+Android | P1 |
| DS-43 | Duplicate review — mixed batch + exclusion | Import batch where some files already exist → review sheet | Sections "Already in your library"/"New" w/ counts; per-row X strikes a file and CTA becomes "Import N"; 0 kept disables CTA; "Skip duplicates" imports only new; Cancel imports nothing | AUTO-A | iOS+Android | P2 |
| DS-44 | Collection document-picker import (single) | Collection → Import audio → pick 1 file → date prompt → name modal → Save | Helper shows destination + date choice; clip appears in collection w/ chosen/fallback title; picker cancel is a silent no-op | AUTO-A | iOS+Android | P1 |
| DS-45 | Collection document-picker import (multi) | Collection → Import audio → pick several files | Same Individual clips / Song project / Cancel choice as share import; both modes land correctly; progress pill tracks n/N and failures | AUTO-A | iOS+Android | P2 |
| DS-46 | Storage details view | Library & Backups → Storage details → wait for measure → Refresh → open Advanced | "Measuring…" panel → report (library total, active/archived workspaces, audio files, library data, archive packages, temp files, all-managed total); Refresh re-measures; header back is a no-op WHILE measuring; Advanced (paths, notes) starts collapsed on every entry | AUTO | iOS+Android | P2 |
| DS-47 | Integrity check — healthy | Storage details → Check integrity | Button shows "Checking…"; "Integrity check passed" alert; "Last check: no problems found." line persists in panel | AUTO | iOS+Android | P2 |
| DS-48 | Integrity check — issues found | Delete a clip's audio file from app storage → Check integrity | "Integrity issues found" + summary (missing audio/broken links); last-check line shows the summary | AUTO-A | iOS+Android | P2 |
| DS-49 | Recover audio files | Library & Backups → Recover audio files (clean library; then with an orphaned audio file planted) | Clean: "No recoverable data" alert; orphan: recovered into a Recovered collection w/ count alert; card shows scanning progress and is disabled while running | AUTO-A | iOS+Android | P2 |
| DS-50 | Settings back mapping | From overview open Library/Recording/About → back; from Library open Export/Import/Storage → back | Library, Recording, About return to overview; Export, Import, Storage return to Library (not overview); overview shows hamburger, subscreens show back arrow | AUTO | iOS+Android | P2 |
| DS-51 | Startup preference | Settings → Startup → pick Last used → relaunch; set a primary workspace → pick Primary → relaunch | Relaunch opens the matching workspace each time; Primary subtitle names the workspace, or shows last-used fallback copy when none set | AUTO | iOS+Android | P2 |
| DS-52 | Haptics toggle | Settings → Feedback → toggle Haptics on/off → relaunch | Turning ON fires a felt confirmation tap; app-wide haptics stop when off; setting persists across relaunch | PARTIAL | iOS+Android | P3 |
| DS-53 | Recording defaults | Settings → Recording defaults → change name-prompt, count-in bars, time signature, click sound, haptic pulse → open record screen | All five persist across relaunch; record screen/metronome start from these defaults; name-prompt OFF saves takes w/ suggested name, ON asks after each take | AUTO | iOS+Android | P2 |
| DS-54 | About page basics | Settings → About | Version row matches app version; privacy statement ("library stays on this device…") visible | AUTO | iOS+Android | P3 |
| DS-55 | Send feedback | About → Send feedback | Mail compose opens w/ subject "SongNook feedback (vX)"; with no mail client configured, silent no-op (no crash) | HUMAN | iOS+Android | P3 |
| DS-56 | Share diagnostic log | About → Share diagnostic log (no crashes recorded; then after a recorded crash) | No log: "No diagnostics recorded" alert; log present: system share sheet offers the JSON log; share failure shows "Could not share" | PARTIAL | iOS+Android | P3 |
| DS-57 | Replay intro | About → Replay intro | Full-screen welcome gate appears immediately; completing it returns to the app with library fully intact | AUTO | iOS+Android | P2 |
| DS-58 | Pro upsell sheet + purchase stub | Settings → SongNook Pro → Upgrade to Pro → select each plan → tap CTA | Sheet lists 6 features + 3 plan cards (annual preselected, "Best value"); CTA reads "Start free trial" / "Get Lifetime" per plan; purchase shows "Purchasing isn't available in this build yet" preview alert (billing stub) | AUTO | iOS+Android | P2 |
| DS-59 | Restore purchases stub | Settings → SongNook Pro → Restore purchases (also from upsell sheet) | "There are no purchases to restore yet." alert; no state change | AUTO | iOS+Android | P2 |
| DS-60 | Pro-active state + legal links | Dev build → force Pro via dev override → view Settings Pro section; tap Active row; on upsell sheet tap Terms/Privacy | Upgrade row replaced by "SongNook Pro · Active"; tapping it opens the store subscription-management URL; Terms/Privacy open in browser | PARTIAL | iOS+Android | P3 |
