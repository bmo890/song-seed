/**
 * Scroll offset (px) over which the compact nav-row title fades in. Tuned to the
 * height of the large title block so the compact title appears exactly as the
 * large title slides up and out — no gap where neither title is visible.
 */
export const COMPACT_TITLE_FADE_IN_START = 48;
export const COMPACT_TITLE_FADE_IN_END = 104;
